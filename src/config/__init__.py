
# specify dependencies
# OS = linux (standard GAE or AWS image)
# Python 2.7
# Memcached, Redis, MySQL?
# prune unnecessary dependencies specified in requirements

import logging


env = 'dev' # temp; make env specific config files + where to specify environment on a machine

oauth_token = '' # app specific permission
log_level = logging.DEBUG

